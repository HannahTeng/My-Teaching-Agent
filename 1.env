# .env 文件内容

# 服务端口
PORT=3000

# === AI Key 配置 (暂时没有就填 placeholder) ===
# 填了 placeholder 后，项目能启动，但点击 AI 功能时会报错，这是正常的。
OPENAI_API_KEY="sk-placeholder-key-for-local-dev"
ELEVENLABS_API_KEY="placeholder-key"

# === 数据库配置 (本地化关键步骤) ===
# 这个项目使用了 Redis 作为 KV 数据库。
# 如果你没有安装 Docker，你需要先安装 Redis，或者使用下面的本地默认地址。
KV_URL="redis://localhost:6379"
